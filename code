#include <bits/stdc++.h>

using namespace std;

int main()
{
    signed long long a, i = 0;
    
    cin >> a;
    
    
    while((a >> i) != 1)
    {
        i ++;
    }
    
    cout << i;
   
    
    return 0;
}
